# 下载
1.之前通过FileZilla传输了一个文件并解压缩，检查是否下载成功（看版本号的方式）输入命令显示没有下载/找不到

  a.直接进入到根目录（命令：cd/），再次查询。

  b.直接进入相关的文件夹，看看是否为空

  c.若上述能查询到和不为空，那么就是环境变量配置有问题，输入"sudo vi /etc/profile"，重新检查是否有打错/打漏的字母/符号，反复修改和重启，直到能在~目录找到为止
